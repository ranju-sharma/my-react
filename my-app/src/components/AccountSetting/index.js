import React from 'react';
import ReactDOM from 'react-dom';
import Style from '../ListPage/style.css';

class AccountSetting extends React.Component {
	render() {
		return (
			<div>
				<div className='container-right'>In the above example, instances of Avatar own instances of PagePic and PageLink. In React, an owner is the component that sets the props of other components. 
					<div>My name</div>
				</div>								
			</div>
		);
	}
}

export default AccountSetting;